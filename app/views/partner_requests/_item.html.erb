<tr>
  <td><%= f.select :name, POSSIBLE_ITEMS.invert.collect, include_blank: 'Select an item' %></td>
  <td><%= f.number_field :quantity, label: false %></td>
  <td>
    <%= f.hidden_field :_destroy, as: :hidden %>
    <%= link_to 'Remove', '#', class: 'remove_record' %>
  </td>
</tr>
